<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project">
        <activity android:name=".quiz_result"></activity>
        <activity android:name=".start_quiz" />
        <activity android:name=".quiz_menu" />
        <activity android:name=".magical_music_player" />
        <activity android:name=".mediaplayer" />
        <activity android:name=".Video" />
        <activity android:name=".text2speech" />
        <activity android:name=".browser" />
        <activity android:name=".special_function" />
        <activity android:name=".Camera" />
        <activity android:name=".calculator" />
        <activity android:name=".LogedIn" />
        <activity android:name=".signUp" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>